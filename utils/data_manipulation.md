# Data Splitting and Cross-Validation Utilities

This module provides utility functions for splitting datasets into training and validation sets, as well as performing k-fold cross-validation — fully compatible with PyTorch tensors.

---

## 📦 Contents

- `split_data()`
- `cross_validation()`

---